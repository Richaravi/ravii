#include<stdio.h>
int main()
{
int leap;
printf("Enter the year");
scanf("%d",&year);
if(year%400==0)
printf("%d the year is leap year",year);
elseif(year%100==0)
printf("%d the year is not leap year",year);
elseif(year4%==0)
printf("%d the year leap year",year);
else
printf("%d the year is not leap year",year);
return 0;
}
